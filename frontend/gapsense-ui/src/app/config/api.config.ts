// Local API URL must match how you run GapSense.API (see launchSettings.json).
// "http" profile → http://localhost:5292 only. "https" profile → https://localhost:7277 and usually http://localhost:5292.
// Default http:5292 so the UI works with the common VS "http" debug target.
export const API_BASE_URL = 'http://localhost:5292';

/** Current filter selections (ids reference mock catalog; replace with API ids later). */
export interface WeakTopicFilterSelection {
  semesterId: string | null;
  batchId: string | null;
  groupId: string | null;
  studentId: string | null;
  moduleId: string | null;
}

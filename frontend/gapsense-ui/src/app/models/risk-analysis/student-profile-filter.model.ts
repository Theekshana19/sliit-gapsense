/** Filter state for student readiness profile (ready for API params). */
export interface StudentProfileFilterState {
  searchQuery?: string;
  semesterId?: string;
  batchId?: string;
  groupId?: string;
  studentId?: string;
}

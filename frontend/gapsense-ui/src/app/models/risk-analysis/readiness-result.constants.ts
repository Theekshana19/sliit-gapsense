/**
 * Matches seeded `ReadinessResults.Id` in the API migration and demo mock data.
 * Replace with route/query param when readiness is loaded per student/attempt from the API.
 */
export const READINESS_RESULT_EXPORT_ID = '00000000-0000-0000-0000-000000000001';
